Hunter（鹰图平台）是奇安信旗下的网络空间测绘引擎，在信息收集中主要用于**资产发现、归属定位和快速打点**，是FOFA的有力补充。

### 🎯 核心信息收集能力

*   **全网资产探测**：持续探测全球互联网设备的IP、端口、服务及协议，覆盖超6.2亿独立IP。
*   **多维度信息检索**：支持从IP、域名、备案企业、证书、网站图标（icon）等多种视角查询资产。
*   **资产归属定位**：通过ICP备案信息，快速反查域名/IP归属企业，或根据企业名称定位其所有域名资产。
*   **精准指纹识别**：内置200+高精度资产标签（如组件名、服务器类型），便于精准筛选目标。
*   **相似资产发现**：利用AI技术，可通过相似网站图标或页面特征，发现散落在各处的同类资产。

### 💡 实用工作流示例

在实际攻防演练或资产梳理中，Hunter通常通过**API**集成到自动化工具链中，**很少直接在Web界面操作**。

1.  **收集目标资产**：在`dddd`这类工具中，先配置好Hunter的API密钥。然后，使用查询语句从Hunter拉取资产并自动扫描。
    ```bash
    # 从Hunter获取备案机构为"目标公司"的资产进行扫描，最多1000条
    ./dddd -t '"icp.name="目标公司"' -hunter
    ```

2.  **反查资产归属**：使用像`Hunter_query`这样的Python工具集，通过域名或IP反查其ICP备案信息，确认资产归属。
    ```bash
    # 查询example.com的备案企业信息
    python hunter_ip.py -d "example.com"
    # 查询企业名下所有域名
    python hunter_icp.py -c "企业全称"
    ```

### 🚀 自动化工具集成

通过API，Hunter可以与多种开源工具联动，实现工作流自动化。
*   **dddd（带带弟弟）**：支持直接从Hunter批量获取资产并进行指纹识别与漏洞探测。
*   **密探**：集成了Hunter、FOFA等引擎，支持资产测绘与扫描模块联动。
*   **Yakit**：其`spacengine`函数库支持编写脚本来调用Hunter搜索。

需要注意的是，使用这些工具通常需要有效的Hunter API密钥，并注意平台的积分和速率限制。

你是准备直接用Hunter官网进行信息收集，还是想通过API把它集成到已有的自动化工具链里？告诉我你的具体使用场景，我可以为你提供更具体的操作指引。
