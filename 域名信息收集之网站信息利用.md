网站信息利用是信息收集中从“外围”转向“内部”的关键一步。此时你已经拿到了主域名和关联域名，接下来就要从网站本身扒信息——包括JS文件、响应头、证书等，往往能挖到开发人员不小心暴露的“隐藏子域”或“内部系统”。

---

我把这部分分成四个模块，每个都给你具体看哪里、用什么命令。

---

一、爬取网站提取子域（JS/资源文件中的隐藏子域）

原理：前端JS里经常硬编码API地址、CDN地址、图片服务器地址，比如 api.julislings.com、img.julislings.com。

手动方式（浏览器）

打开 https://julislings.com → F12 开发者工具 → Network（网络） 标签 → 刷新页面 → 查看所有加载的JS/CSS/图片的域名，揪出非主域的子域。

命令行自动化（工具）

工具 命令 作用
Subfinder subfinder -d julislings.com -recursive 递归爬取，命中率高
Katana katana -u https://julislings.com -d 5 -o urls.txt 爬取所有页面URL
Gospider gospider -s https://julislings.com -o output 爬取JS/静态资源，自动提取子域
Hakrawler echo "https://julislings.com" | hakrawler -subs 轻量级爬取，专门捞子域

```bash
# 常用组合拳：爬取JS文件并提取子域
gospider -s https://julislings.com | grep -Eo "[a-zA-Z0-9.-]+\.julislings\.com" | sort -u
```

输出示例（常见隐藏子域）：

```
api.julislings.com
cdn.julislings.com
static.julislings.com
dev-api.julislings.com
admin.julislings.com
```

---

二、安全策略泄露信息（CSP / 跨域 / 响应头）

1. CSP（内容安全策略）——最容易泄露

CSP在响应头 Content-Security-Policy 中，它的 connect-src、script-src、img-src 等指令会列出所有允许加载资源的域名。

查看方式：

```bash
curl -I https://julislings.com | grep -i "content-security-policy"
```

示例（假设返回）：

```
Content-Security-Policy: default-src 'self'; connect-src api.julislings.com wss://ws.julislings.com; script-src cdn.julislings.com
```

🔥 这里直接暴露了 api.julislings.com、ws.julislings.com、cdn.julislings.com，比爬虫还准。

2. CORS（跨域资源共享）——暴露信任域

响应头中的 Access-Control-Allow-Origin 如果配置了具体域名，会暴露信任域。

```bash
curl -I https://julislings.com | grep -i "access-control"
```

如果返回 Access-Control-Allow-Origin: https://admin.julislings.com，说明存在跨域互信，把 admin.julislings.com 加入目标池。

3. Server / X-Powered-By ——暴露技术栈

```bash
curl -I https://julislings.com | grep -iE "server|x-powered-by|via"
```

输出示例：

```
Server: nginx/1.18.0
X-Powered-By: PHP/7.4.33
```

→ 知道是Nginx + PHP，后续可以针对性找对应版本的已知漏洞。

---

三、SSL/TLS证书关联域名（证书里的SAN字段）

原理：一张证书可以绑定多个域名（如 example.com、www.example.com、api.example.com），这些域名都会出现在证书的 Subject Alternative Name (SAN) 字段中。

命令行查证书（不需要浏览器）

```bash
# 方法1：用openssl
echo | openssl s_client -servername julislings.com -connect julislings.com:443 2>/dev/null | openssl x509 -noout -text | grep -A10 "Subject Alternative Name"

# 方法2：用certwatch（更干净）
curl -s https://crt.sh/?q=julislings.com | grep -oP '(?<=<TD>)[^<]*\.julislings\.com' | sort -u
```

输出示例：

```
DNS:julislings.com
DNS:www.julislings.com
DNS:api.julislings.com
DNS:staging.julislings.com
DNS:mail.julislings.com
```

🔥 有时连开发环境 test.julislings.com、dev.julislings.com 都会绑在同一张证书上，直接暴露。

浏览器查看（手动）

1. 访问 https://julislings.com
2. 点地址栏的🔒锁 → 证书 → 详细信息 → 主题备用名称

---

四、用工具一键完成以上所有（省时间）

工具 命令 覆盖功能
WhatWeb whatweb https://julislings.com 技术栈识别 + 响应头 + Cookie
Wappalyzer 浏览器插件 一键识别框架/语言/服务器
SecurityHeaders https://securityheaders.com/?q=julislings.com 全响应头安全评分，直接显示CSP内容
SSL Labs https://www.ssllabs.com/ssltest/analyze.html?d=julislings.com 证书全信息 + SAN列表

---

五、一句话总结

看响应头拿CSP/CORS/Server，爬JS拿API子域，看证书SAN拿所有绑定域名。

这三样扫完，往往能比WHOIS和搜索引擎多挖出 3~10个内部子域（比如 staging、dev、ws、mail），这些都是后续渗透的高价值目标。

---

完整信息收集链路（到目前为止）

阶段 方法
① WHOIS 注册人/邮箱/NS/过期时间
② 搜索引擎 site/inurl/filetype/GitHub
③ 第三方查询 CrtSearch/DNSDumpster/VirusTotal
④ ASN关联 查ASN扫所有IP段
⑤ 域名相关性 邮箱反查/备案反查/迭代
⑥ 网站信息利用 爬JS/响应头/证书SAN

这套链条跑完，你的攻击面已经扩大了 5~20倍。

---

下一步：如果要直接上手练，我可以带你拿一个合法授权目标走一遍“从0到完整资产清单”，还是想先学 绕过CDN找真实IP？