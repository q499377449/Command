FOFA 在信息收集方面的核心能力，正是围绕你提到的 **IP、域名、端口和指纹** 这四个维度展开的，可以理解为一个面向互联网所有公开资产的强大搜索引擎。

下面是针对这四个维度的具体信息收集方法和常用语法：

### 🎯 核心信息收集维度与语法

#### 🌐 IP 与 域名
*   **IP地址与网段**：通过 `ip="IP地址"` 查询特定IP的资产，或使用 `ip="IP段/24"` 查询整个C段IP的资产。
*   **域名与子域名**：通过 `domain="example.com"` 搜索该主域名下的所有子域名和相关资产，是子域名收集的有效手段。

#### 🔌 端口
*   **端口搜索**：使用 `port="端口号"` 搜索所有开放了特定端口的资产。例如，`port="3389"` 用于搜索开放了远程桌面的主机。
*   **组合端口**：可以使用 `ports="80,443"` 来搜索同时开放了80和443端口的IP地址。

#### 🖐️ 指纹
*   **服务与组件指纹**：直接使用 `app="组件名"` 或 `server="服务器名"` 来查找使用了特定软件或服务的资产。例如，`app="海康威视-视频监控"` 可以找到网络摄像头。
*   **标题与内容指纹**：
    *   `title="关键词"`：搜索网页标题中包含特定关键词的页面。
    *   `body="关键词"`：搜索网页正文内容中包含特定关键词的页面。
    *   `header="关键词"`：搜索HTTP响应头中包含特定关键词的资产。
*   **证书指纹**：使用 `cert="证书内容"` 来搜索使用了特定SSL证书的资产，这对于关联同一个实体控制下的不同资产非常有效。

### 💡 实用工作流示例

掌握了基础语法后，就可以把它们组合起来，形成高效的搜索工作流：

1.  **针对某企业的资产梳理**
    ```bash
    # 搜索 example.com 域名下，开放了 443 端口且标题包含 "后台管理" 的 HTTPS 服务
    domain="example.com" && port="443" && title="后台管理"
    ```

2.  **查找特定组件的漏洞影响面**
    ```bash
    # 搜索公网上所有使用了 Apache Log4j 组件的资产
    app="Log4j2"
    ```

3.  **通过图标进行精准定位**
    ```bash
    # 结合 icon_hash 进行搜索，这通常能非常精准地找到使用了相同 favicon 的同一套系统
    icon_hash="443881024"
    ```

### 🚀 自动化工具

除了在官网手动搜索，你还可以使用 API 进行自动化信息收集。例如，GitHub 上的开源项目 `fofa_collector` 可以通过配置 API 密钥，自动执行查询并分类保存结果，将结果自动整理到 `domain.txt`、`subdomain.txt`、`ip.txt` 和 `url.txt` 等文件中。
